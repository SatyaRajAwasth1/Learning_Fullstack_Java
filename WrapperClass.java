public class WrapperClass {
    // wrapper class -----> provides a way to use primitive data types as reference data types
    // reference data types contains usefull  methods
    // can be used with collections (Eg. ArrayList)
    public static void main(String[] args) {

        /*
        ----In Primitive----                      ----In Wrapper----
        boolean                                   > Boolean
        char                                      > Character
        int                                       > Integer
        double                                    > Double
         */

        // autoboxing ---> Feature that automatically converts primitive into corresponding wrapper class objects
        // Defined as automatic conversion that the Java compiler makes between the primitive types and their corresponding object wrapper classes. For example, converting an int to an Integer, a double to a Double, and so on.
        //unboxing ---> If the conversion goes the other way, this is called unboxing.
        //thats Automatically converting an object of a wrapper class to its corresponding primitive type is known as unboxing. For example – conversion of Integer to int, Long to long, Double to double etc.

    }
}
